<div class="container live-page">
  <div class="row">
    <div class="box small-12 medium-10 large-10">
    <h2>UPCOMING SHOWS</h2>
    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>City</th>
          <th>Venue</th>
          <th>Alongside</th>
          <% if user_signed_in? %>
            <td><h4>Edit/Delete</h4></td>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% @tours.each do |t| %>
          <tr>
          <% if t.date != nil %>
          <td><%= t.date.strftime("%D") %></td>
          <% end %>
          <td><%= t.city %></td>
          <td><a href="<%= t.url %>" target="_blank"><%= t.venue %></a></td>
          <td><%= t.bands %></td>
          <% if user_signed_in? %>
            <td><div>
              <%= button_to "Edit", edit_tour_path(t.id), :method => "get", action: "edit", :class => "tiny radius button" %>
              <%= button_to "Delete", tour_path(t.id), :method => "delete", data: {confirm: "Are you sure?"}, :class => "tiny radius button" %>
            </div></td>
          <% end %>
          <!-- <hr> -->
        <% end %>
        </tr>
      </tbody>
    </table>
   </div>
  </div>
</div>
